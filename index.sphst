const fm = _.frontmatter({
    title: "Berkay's Space on the Internet",
});

_.headLinkCSS('/style.css');

const { Header } = await _.import('./components.js');

_.html(Header());

_.html(`<main><div>`);

_.h1(fm.title);

_.html(`<p>Hi there, this is my website where I write and share things.</p>`);

_.html(`</div></main>`);