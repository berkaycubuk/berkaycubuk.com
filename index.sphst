_.headLinkCSS('/style.css');

const title = "Berkay's Space on the Internet";

_.head('<title>' + title + '</title>');

const { Header } = await _.import('./components/header.js');

_.html(Header());

_.html(`<main><div>`);

_.h1(title);

_.html(`<p>Hi there, this is my website where I write and share things.</p>`);

_.html(`</div></main>`);